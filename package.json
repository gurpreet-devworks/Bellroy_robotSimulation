{
	"name": "robotapp",
	"version": "1.0.0",
	"description": "a Javascript application in which you can simulate controlling a robot. The robot should be placed on a 5x5 grid. Supply controls that let you move the robot forwards in the direction it is facing, and rotate the robot in place to face any cardinal direction.",
	"main": "main.js",
	"scripts": {
		"start": "live-server src",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": ["Robot Simulation with keyboard support"],
	"author": "Gurpreet Kaur",
	"license": "ISC",
	"dependencies": {
		"http-server": "^14.1.1"
	},
	"devDependencies": {
		"live-server": "^1.2.2"
	}
}
